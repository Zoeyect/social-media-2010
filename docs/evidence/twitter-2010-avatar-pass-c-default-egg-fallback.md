# Twitter Final QA — Avatar Pass C Default Egg Fallback

Status: implemented; pending manual native-scale QA.

## Evidence classification

- Twitter egg as the target-date default-avatar concept for October 20–21, 2010: `PERIOD-SUPPORTED / PROBABLE`.
- Exact Twitter for iPhone 3.0.x raster: `HOLD`.
- Exact palette: `HOLD`.
- Project artwork: `RECONSTRUCTED_FROM_PERIOD_SCREENSHOT / period evidence`.

The SVG is not authenticated Twitter application-bundle artwork. It conservatively reconstructs the period-supported centered pale egg on one blue field. The historical multi-color set and account-color assignment remain `HOLD` and are not simulated.

## Reconstructed asset

- Canvas: 48×48.
- Background: `#91b7ca` (`RECONSTRUCTED`).
- Egg: `#f3f0e7` (`RECONSTRUCTED`).
- Egg visible bounds: approximately x=13–35, y=7.5–40.5.
- No initials, face, human silhouette, bird, shadow, filter, gradient, or circular crop.

The existing 48×48 frame, 4px radius, 1px border, and all surface-specific row geometry remain unchanged.

## Resolution contract

Resolution remains centralized:

1. approved Avatar Pass A canonical mapping;
2. future period-authenticated public-account mapping;
3. reconstructed period default egg.

The default applies to Jay Diaz; Dana, Nora, Mia, Marcus, Eli, Claire, Sam, Priya, and Eva; the session owner; dynamic public visitors; Conan's linked-Tweet identity; and unresolved Suggested Users/public accounts. Disabling the temporary canonical name bridge for session-owner and public-visitor rows still returns the egg and never a fictional portrait.

If an approved canonical image fails to load, the shared renderer changes to the same egg descriptor. It does not reveal initials underneath.

## Boundaries

Avatar Pass A mappings and crops for June, Matt, Jack, Alex, Ben, Katie, Chris, and Luca remain frozen.

Suggested Users still require optional Avatar Pass B historical research before account-specific public imagery can be considered final. The egg removes visible prototype leakage but is only an interim state for those established public accounts. No modern logos were introduced.

The fallback does not add visitor Profiles, Follow actions, verification, statistics, or account ownership. Public Visitor capability gating remains unchanged.
